# Asset System

This file is intentionally a placeholder until the asset system is implemented.

When populated, document:

- asset identity;
- asset versioning;
- semantic metadata;
- style-version interaction;
- lookup/search behavior;
- compatibility expectations;
- asset usage tracking;
- reproduction requirements.

Do not use filenames alone as stable asset identity.
